apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    app.kubernetes.io/managed-by: modeldynamics
  name: sk-learn-random-forest-reg3
  namespace: argocd
spec:
  destination:
    namespace: helix-project-0
    server: https://kubernetes.default.svc
  project: default
  source:
    path: ./
    repoURL: git@github.com:lukemarsden/helix-test
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
